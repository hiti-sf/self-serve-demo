// Standalone module: the launcher has no dependencies beyond the Go standard library,
// which is the point — a booth laptop needs nothing installed to run a kiosk bundle.
module github.com/pivotpath/demo-platform/kiosk-launcher

go 1.22
